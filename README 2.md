# Santander Value Prediction Challenge

**Соревнование:** [Santander Value Prediction Challenge](https://www.kaggle.com/c/santander-value-prediction-challenge)
**Задача:** регрессия — предсказать значение транзакции клиента
**Метрика:** RMSLE (Root Mean Squared Logarithmic Error)

## Результат

| | Public Score | Private Score |
|---|---|---|
| Финальный сабмит | **1.45035** | 1.40890 |

Скриншот результата в папке(кнопка увидить место не работает, % не рассчитан)
Финальный файл: `submission.csv` ы

## Данные
Данные слишком много весят, gitHub не принял.
Скачать с [страницы соревнования Kaggle](https://www.kaggle.com/c/santander-value-prediction-challenge/data),
положить `train.csv` и `test.csv` в папку `santander-value-prediction-challenge/`.

- train: 4459 строк × 4992 колонки (все признаки числовые, анонимизированные)
- test: 49342 строки
- Целевая переменная сильно скошена: от 30 тыс. до 40 млн, медиана 2.26 млн

## Подход: baseline → улучшения → финал

| Шаг | Что сделал | CV RMSLE | Public LB |
|---|---|---|---|
| 1 | LightGBM + SelectKBest(f_regression, k=300) | 1.4373 | 1.47362 |
| 2 | Отбор признаков по feature_importances_ LightGBM, k=300 | 1.3552 | — |
| 3 | XGBoost, CatBoost, RandomForest, Lasso по отдельности | 1.3607 / 1.3666 / 1.3886 / 1.6279 | — |
| 4 | Стекинг 5 моделей (LinearRegression, positive=True) | 1.3402 | **1.45035** |

### 1. EDA

- Все 4992 признака числовые, пропусков в train нет
- Удалены константные признаки: 4992 → 4735
- Целевая переменная логарифмирована (`np.log1p`) — это одновременно и требование метрики (RMSLE = RMSE в лог-пространстве), и техническая необходимость (см. раздел «Ошибки»)
- Корреляции признаков с таргетом слабые: максимум 0.27, нет ни одного доминирующего предиктора

### 2. Feature Engineering

Датасет разреженный (много нулей), поэтому построчные агрегаты несут информацию о «масштабе» клиента:

- `n_zeros` — количество нулей в строке
- `row_sum` — сумма по строке
- `row_mean` — среднее по строке
- `row_max` — максимум по строке

`row_mean` и `row_sum` попали в топ-10 по корреляции с таргетом (0.272 и 0.268) — то есть оказались сильнее большинства исходных признаков.

### 3. Отбор признаков

Сравнил два подхода:

- `SelectKBest(f_regression)` — линейный критерий, дал 1.4373
- Отбор по `feature_importances_` LightGBM — дал 1.3552

Второй заметно лучше, потому что деревья учитывают нелинейности и взаимодействия, которые линейная корреляция не видит.

Подбор `k` через CV:

| k | CV RMSLE |
|---|---|
| 200 | 1.3718 |
| **300** | **1.3552** |
| 400 | 1.3581 |
| 500 | 1.3586 |
| 1000 | 1.3809 |
| 2000 | 1.4083 |
| 4740 (все) | 1.4321 |

Оптимум — 300 признаков.

### 4. Модели и ансамбль

Перед стекингом проверил корреляции предсказаний моделей (через `cross_val_predict`):

|  | xgb | cbt | lgbm | rf | l1 |
|---|---|---|---|---|---|
| xgb | 1.00 | 0.96 | 0.97 | 0.93 | 0.56 |
| cbt | 0.96 | 1.00 | 0.95 | 0.92 | 0.58 |
| lgbm | 0.97 | 0.95 | 1.00 | 0.92 | 0.55 |
| rf | 0.93 | 0.92 | 0.92 | 1.00 | 0.50 |
| l1 | 0.56 | 0.58 | 0.55 | 0.50 | 1.00 |

Три бустинга коррелируют между собой на 0.95+ — ансамбль из них одних даёт слабый прирост. RandomForest чуть разнообразнее (0.92), а Lasso — принципиально другая модель (0.50–0.58), потому что не ловит взаимодействия признаков.

Веса стекера (LinearRegression с `positive=True`):

```
lgbm: 0.647 | cbt: 0.189 | rf: 0.140 | xgb: 0.043 | l1: 0.030
```

## Что сработало

1. **Логарифмирование таргета** — самое влиятельное изменение. Не только по метрике, но и по скорости: обучение Lasso ускорилось с 141 сек до 0.39 сек.
2. **Отбор признаков по importance вместо корреляции** — 1.4373 → 1.3552.
3. **Сокращение до 300 признаков** из 4740 — меньше переобучения при p ≈ n.
4. **Добавление Lasso в ансамбль** — единственная модель с низкой корреляцией предсказаний, дала реальное разнообразие.

## Что не сработало

1. **Стекинг трёх бустингов между собой** — корреляция предсказаний 0.95+, прироста почти нет. Проверка корреляций до стекинга сэкономила время.
2. **Дополнительные построчные признаки** (`row_std`, `row_median`, `row_skew`, `nonzero_mean`, `n_nonzero`) — CV изменился с 1.3402 до 1.3391, то есть в пределах шума. Зато `nonzero_mean` внёс NaN и сломал пайплайн, поэтому от расширенного FE отказался.
3. **Lasso как самостоятельная модель** — 1.6279, заметно хуже деревьев: линейная модель не справляется с этой задачей в одиночку.
